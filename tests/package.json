{
	"name": "arcane-tests",
	"type": "module",
	"scripts": {
		"preinstall": "npx only-allow pnpm",
		"check": "tsc --noEmit",
		"format": "vp fmt",
		"format:check": "vp fmt --check",
		"test": "playwright test"
	},
	"devDependencies": {
		"@playwright/test": "^1.62.1",
		"@types/node": "^26.4.0",
		"typescript": "^6.0.3",
		"vite": "catalog:",
		"vite-plus": "catalog:"
	}
}
